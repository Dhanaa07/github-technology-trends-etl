# Github_repoAnalysis
Build an end-to-end Data Engineering solution that ingests GitHub repository data via REST APIs, stores raw data in a Bronze layer, transforms it into curated Silver tables, and produces Gold analytical datasets for repository, language, and topic insights using Spark and Delta Lake.
